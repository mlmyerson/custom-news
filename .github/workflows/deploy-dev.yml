name: (Retired) development deployment

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Development deploy retired
        run: |
          echo "Development deployments now follow the main workflow."
          echo "This workflow is kept only to document the change."
